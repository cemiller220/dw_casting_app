import { createStore } from 'vuex'
import cast_list from "./cast_list";

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    cast_list: cast_list
  }
})
